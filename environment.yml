name: compSPI
channels:
  - conda-forge
  - defaults
dependencies:
  - codecov
  - configparser
  - coverage
  - numpy
  - pip
  - pytest
  - pytest-cov
  - pip:
    - git+https://github.com/compSPI/ioSPI.git
    - git+https://github.com/compSPI/simSPI.git